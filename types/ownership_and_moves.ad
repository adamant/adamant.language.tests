public fn main()
{
    let book: Book = new Book();
    take(book); // ERROR need `move`
    let borrow: Book = book;
    take(move borrow); // ERROR can't move from a borrow

    take(move book);
    let borrow = book; // ERROR use of moved value
}

public class Book
{
}

public fn take(book: Book$owned)
{
}
