public fn main()
{
    let book: Book$owned = new Book();
    take(book); // ERROR need `move`
    let borrow: Book = book;
    let moved_book1: Book$owned = book; //ERROR
    let moved_book2: Book$owned = move book; //ERROR
    take(move borrow); // ERROR
}

public mut class Book
{
}

public fn take(book: mut Book$owned)
{
}
